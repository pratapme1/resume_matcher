// Placeholder entry so Vercel's `functions` config matches a tracked file.
// `scripts/build-api.mjs` overwrites this file with the bundled handler during builds.
export { default } from './_index.ts';
